using System;

public class Program {
  public static bool Puzzle(bool x, bool y, bool z) {
	  
	  switch(x)
	  {
		  case x:
		  return x;
		  case true:
		  return true;
	  }
	  //if (!(x || y || z)) return false;
	  //else return true;
    //return (x || y && z) || ((x) && (y || z));
  }
}