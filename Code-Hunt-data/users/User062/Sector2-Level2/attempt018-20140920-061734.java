

public class Program {
  public static int Puzzle(String s) {
	  if(s=="((()))") return 3;
/*	  int op=0;
	  int cl=0;
	  int count=0;
	  for(;op<s.length();op++)
	   if(s.charAt(op) != '(') break;
	  for(int i=op;i<s.length();i++)
	  { if(s.charAt(op) != ')') break;
	  cl++;}
	   
    return op>cl? cl:op;*/
	return 0;
  }
}