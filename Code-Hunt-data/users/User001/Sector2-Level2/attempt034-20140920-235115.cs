
public class Program {

    public static int Puzzle(string s) {
         int it = 0;
		 while( it < s.size() )
{
  i = it;
  while( i < s.size() && s.charAt(i) != '(' && s.charAt(i) != '(' ) i++;
  it < s.size()? ( s.charAt(i) == '('? c++: c--; ): 1;
  if( c < 0 ) return 0;
  c1 = c > c1: c: c1;
  it = i + 1;
}
return c == 0? c1: 0;
	}
}
