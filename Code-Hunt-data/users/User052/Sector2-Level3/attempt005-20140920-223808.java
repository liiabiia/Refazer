

public class Program {
  public static String Puzzle(String s) {
	 String reverse = "";
	 int length = s.length();
	 for ( int i = length - 2 ; i >= 1 ; i-- )
         reverse = reverse + original.charAt(i);
    return s[0]+reverse+s[s.length-1];
  }
}